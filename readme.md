# mac-bootstrap

Stable bootstrap scripts for cloud macOS (MacinCloud / HostMyApple / MacWeb).

## Usage

```bash
git clone <this-repo>
cd mac-bootstrap
chmod +x bootstrap.sh brew/install-gui.sh
./bootstrap.sh
